# 채용 공고 서비스 요구사항

## 1. 공고 자동 등록

- **5분마다** 공고 **50건**을 자동 등록하여 저장한다.
- 공고는 다음 정보를 가진다: **제목**, **기업 정보**, **등록 일시**, **조회수**
- 데이터 내용은 자유롭게 구성한다.
- 공고 수정/삭제 기능은 필요 없다.

계획 5분마다라는 이야기 나왔음으로 스케줄링을 통해서 5분 마다 자동 등록
데이터 내용은 자유롭게 임으로 아무렇게나 반복 생성

구현 자체는 스케줄러로 구현이 어렵지 않으나 sql에서 save all을 사용 할 경우 
총 50번이 쿼리가 나간다. 이것 자체는 부담은 아니지만 공고 목록과 이어지는 트래픽을 생각하면 쓰기 작업은 부담을 최소화 해야 한다.
jdbc템플릿으로 빠르게 하는 방법이 있지만 jpa 기능과 다른 엔티티의 확장성을 생각하면 시퀀스를 사용하여 여러 엔티티들이 같이 사용하는 패턴이 베스트라고 판단했다.

## 2. 공고 목록 API

- 등록된 공고를 조회하는 API
- **커서 기반 페이지네이션** 방식
  - 전체 건수는 세지 않는다.
  - 마지막으로 받은 항목으로부터 이어서 조회한다.
  - 다음 페이지 존재 여부(`hasNext`)를 응답에 포함한다.


커서 기반임으로 무겁게 페이지보다는 슬라이스를 사용한다.
## 3. 메인 페이지 공고 목록 API

- 메인 페이지에 노출할 공고 목록 **최대 100건**을 반환하는 API
- 노출 기준은 자유이며, **10분 주기**로 갱신된다. (선택한 기준은 README에 기재)
- 각 공고에는 **제목**과 **조회수**가 포함된다.
- 목록은 10분 주기로 갱신되지만, **조회수는 최신값**(조회 후 몇 초 이내)이어야 한다.
- 해당 API는 **매우 높은 동시 접속**을 가정하여 성능을 고려한다.

## 4. 공고 조회 이력 등록 API

- 호출 시 해당 공고의 **조회수가 1 증가**하고, **조회 이력 1건이 등록**된다.
- 같은 유저가 여러 번 호출해도 호출한 만큼 모두 기록한다.
- **기록 상한**: 공고당 **100회**
  - 조회수와 조회 이력 모두 총 100회까지만 기록한다.
  - **1,000명이 동시 접속**하여도 최종 조회수는 정확히 **100**이어야 한다.
  - 조회수와 이력 건수는 항상 일치해야 한다.

### 반영 시점

- 조회수와 이력은 호출 즉시 처리하지 않아도 된다.
- 단, **몇 초 안에** 목록/메인 API에 반영되어야 한다.
- 목표 반영 시간을 README에 기재한다.

### 구현 방법

- 자유롭게 선택하되, 선택한 방법과 이유를 README에 기재한다.