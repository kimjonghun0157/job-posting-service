# 채용 공고 서비스 요구사항

## 1. 공고 자동 등록

- **5분마다** 공고 **50건**을 자동 등록하여 저장한다.
- 공고는 다음 정보를 가진다: **제목**, **기업 정보**, **등록 일시**, **조회수**
- 데이터 내용은 자유롭게 구성한다.
- 공고 수정/삭제 기능은 필요 없다.

**접근 방식**

`@Scheduled`로 5분 주기 자동 등록. `saveAll()` 사용 시 건별 INSERT 50회가 나가는데, 공고 목록 API와 겹치는 트래픽을 생각하면 쓰기 부하를 줄여야 했다. JdbcTemplate 벌크 INSERT가 빠르긴 하지만 JPA 기능(변경 감지, 연관 매핑)과 다른 엔티티(ViewHistory 등)의 확장성을 고려해 **공유 시퀀스** (`allocationSize=100`) 패턴을 선택했다. 시퀀스를 100 단위로 선점하니 50건 INSERT에 ID 채번 쿼리가 1회로 줄고, `batch_size=100`으로 실제 INSERT도 한 번에 묶인다.

## 2. 공고 목록 API

- 등록된 공고를 조회하는 API
- **커서 기반 페이지네이션** 방식
  - 전체 건수는 세지 않는다.
  - 마지막으로 받은 항목으로부터 이어서 조회한다.
  - 다음 페이지 존재 여부(`hasNext`)를 응답에 포함한다.

**접근 방식**

커서 기반이니 Pageable/Page 대신 가벼운 Slice 스타일로 구현했다. `size+1`건을 조회해서 초과 여부로 `hasNext`를 판단하므로 COUNT 쿼리가 없다. 정렬은 `CursorSort`로 허용 필드(`id`, `createdAt`)만 받고, Pageable의 무제한 Sort는 쓰지 않는다.

## 3. 메인 페이지 공고 목록 API

- 메인 페이지에 노출할 공고 목록 **최대 100건**을 반환하는 API
- 어떤 공고를 노출 할지는 ㅇ노출 기준은 자유이며, **10분 주기**로 갱신된다. (선택한 기준은 README에 기재)
- 각 공고에는 **제목**과 **조회수**가 포함된다.
- 목록은 10분 주기로 갱신되지만, **조회수는 최신값**(조회 후 몇 초 이내)이어야 한다.
- 해당 API는 **매우 높은 동시 접속**을 가정하여 성능을 고려한다.

**접근 방식**

노출 기준으로 **10분간 조회 급상승 트렌딩**을 선택했다. 조회수 상한이 100이라 단순 누적 조회수로는 순위 변별이 어렵고, 채용 사이트 특성상 "지금 관심 몰리는 공고"가 사용자에게 더 유용하다고 봤다. Redis Sorted Set(`ZINCRBY`)으로 실시간 랭킹을 집계하고, 10분 주기로 상위 100건 ID를 Redis List에 캐시한 뒤 랭킹을 초기화한다. 조회수는 매 API 호출 시 Redis에서 바로 읽어 실시간 반영. 목록 ID는 캐시, 조회수는 Redis, DB에서는 제목만 가져오니 높은 동시 접속에도 응답이 빠르다.

## 4. 공고 조회 이력 등록 API

- 호출 시 해당 공고의 **조회수가 1 증가**하고, **조회 이력 1건이 등록**된다.
- 같은 유저가 여러 번 호출해도 호출한 만큼 모두 기록한다.
- **기록 상한**: 공고당 **100회**
  - 조회수와 조회 이력 모두 총 100회까지만 기록한다.
  - **1,000명이 동시 접속**하여도 최종 조회수는 정확히 **100**이어야 한다.
  - 조회수와 이력 건수는 항상 일치해야 한다.

### 반영 시점

- 조회수와 이력은 호출 즉시 처리하지 않아도 된다.
- 단, **몇 초 안에** 목록/메인 API에 반영되어야 한다.
- 목표 반영 시간을 README에 기재한다.

### 구현 방법

- 자유롭게 선택하되, 선택한 방법과 이유를 README에 기재한다.

**접근 방식**

1,000명 동시 접속에서 정확히 100이라는 조건이 핵심이다. 어플리케이션 레벨 락이나 DB 비관적 락으로는 병목이 생기거나 경합에서 밀리는 요청이 나온다. **Redis INCR 원자적 카운터**로 해결했다.

흐름은 이렇다:
1. Redis `INCR view:count:{id}` → 반환값이 100 이하면 통과, 초과면 `DECR`로 복원하고 거부
2. 통과한 요청만 Redis List 큐에 `{postingId}:{userId}:{seqNumber}` 형태로 적재
3. 1초 주기 스케줄러가 큐에서 최대 500건씩 꺼내 DB에 반영 (viewCount UPDATE + ViewHistory INSERT)
4. DB 제약조건(`CHECK seq_number BETWEEN 1 AND 100`, `UNIQUE(job_posting_id, seq_number)`)이 최종 안전장치

Redis INCR은 단일 스레드에서 원자적으로 처리되니 1,000개 스레드가 동시에 쏴도 정확히 100번째에서 끊긴다. 조회수는 Redis에서 바로 읽으니 API 응답에 즉시 반영되고, DB 이력은 1초 이내에 뒤따라 반영된다.

동시성 검증: `ViewHistoryCommandServiceTest.concurrentAccessGuaranteesExactly100()` — `ExecutorService`로 1,000스레드를 동시에 돌려 최종 viewCount=100, historyCount=100을 확인한다.

---

## 평가 기준 메모

- 단일 인스턴스 기준 평가, 확장 고려 설계나 한계 설명 시 가점
- 제출물: 소스 저장소 전체 (도커 yml 포함), README (프로젝트 소개·API 설명·기술적 이유·실행 방법), AI 활용 리포트
- 선택 제출: 동시성 검증 자료 (테스트 코드, 부하 스크립트)
- 핵심 평가 포인트: 기능 구현을 넘어 동시성·대량 트래픽 등 비기능 요구사항을 어떻게 풀었는지
