# AI 활용 리포트

## 활용 범위

코드 생성, 리팩토링, 테스트 작성, 부하 테스트 스크립트, 문서화에 AI를 활용했습니다.  
요구사항 해석과 설계 판단은 직접 했고, AI는 그 판단을 코드로 옮기는 데 썼습니다.

## 내가 판단한 것

- **아키텍처**: Redis 인프라를 도메인에서 분리하기 위해 Port/Adapter 구조를 채택. `ViewCountRedisService` 하나에 몰려 있던 카운팅·큐·랭킹·캐시 역할을 포트별로 나눔
- **Redis 자료구조 선택**: 조회수는 INCR, 큐는 List, 랭킹은 Sorted Set, 캐시는 List, 중복 판별은 Set — 각 역할에 맞는 자료구조를 직접 결정
- **트렌딩 기준**: 단순 누적 조회수가 아닌 10분 단위 급상승 공고를 메인에 노출하기로 판단. 조회수 상한(100)이 있어 누적 방식은 의미 있는 순위를 만들기 어려웠음
- **커서 기반 페이지네이션**: offset 대신 커서 방식을 선택하고, 정렬 기준을 `CursorSort`로 제한하여 안정적인 페이지네이션을 보장
- **동시성 전략**: Redis INCR 원자성 + DB CHECK/UNIQUE 제약으로 이중 안전장치 구성
- **랭킹 신뢰성**: 봇·광클릭 방어를 위해 UniqueViewStore 도입을 결정. 조회수·이력은 전수 기록하되 랭킹 점수만 유저당 1회로 제한
- **스케줄러 분리**: `@Scheduled`를 서비스에서 빼고 스케줄러 클래스가 서비스 메서드를 호출하는 구조로 변경
- **DB 제약조건**: JPA 어노테이션이 아닌 SQL 파일로 관리

## 검토·수정한 것

- AI가 생성한 코드는 직접 리뷰 후 반영. 불필요한 추상화나 과도한 에러 처리는 제거
- 부하 테스트 프로파일(Tomcat 스레드, HikariCP 커넥션 풀)은 AI 제안값을 로컬 환경에 맞게 조정
- 테스트 시나리오의 VU 수, 실행 시간, 기대값을 직접 설정
- README는 AI 초안을 기반으로 구조와 표현을 수정
